name: Exploring Variables and Secrets
#env at workflow level
env:
  CONTAINER_REGISTRY: docker.io
  IMAGE_NAME: github-actions-nginx
  #user-name is stored as variable in repo level

on:
   workflow_dispatch:

jobs:


  docker:
    runs-on: ubuntu-latest

    steps:
      - name: Docker Build
        run: echo docker build -t $CONTAINER_REGISTRY/${{ vars.DOCKER_USERNAME }}/$IMAGE_NAME:latest

      - name: Docker Login
        #password is stored as secret in repo level
        run: echo docker login --username=${{ vars.DOCKER_USERNAME }} --password=${{ secrets.DOCKER_PASSWORD }}

      - name: Docker Publish
        run: echo docker push $CONTAINER_REGISTRY/${{ vars.DOCKER_USERNAME }}/$IMAGE_NAME:latest

  deploy:
    if: github.event_name == 'workflow_dispatch'
    needs: docker
    concurrency:
      #if mentioning true: latest trigger runs by overriding in-progress
      #if mentioning false: latest trigger runs after in-progress is completed
      group: prod-deploy
      cancel-in-progress: false
    runs-on: ubuntu-latest

    steps:
      - name: Docker Run
        #fails workflow if the process is not completed within mentioned time
        timeout-minutes: 2
        run: echo docker run -d -p 8080:80 $CONTAINER_REGISTRY/${{ vars.DOCKER_USERNAME }}/$IMAGE_NAME:latest